---
sidebar_position: 2
title: Nexys.clear()
---

# Nexys.clear()

Returns `Promise<void>`.

---

Clears all data inside `logPool` and `requests`. Also resets `localStorage`.

## Example

```javascript
nexys.clear();
```